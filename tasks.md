# Задачи MyDebate

#todo #project

План проекта: [[PLAN]] · Прогресс: [[progress]]

---

## Этап 0 — Каркас проекта #done
- [x] Инициализировать Next.js **16** (App Router) + TypeScript *(Turbopack по умолчанию)*
- [x] Настроить Tailwind **v4** (конфиг через `@theme` в globals.css)
- [x] Завести дизайн-токены из макетов (бренд-синий, navy, поверхности, радиусы)
- [x] Базовые UI-компоненты: Button, Input, Card, Badge, Container (+ утилита `cn`)
- [x] Общий Navbar (адаптивный, бургер) + Footer (4 колонки)
- [x] Корневой layout (шрифт Inter + кириллица, ru-локаль, метаданные)
- [x] Логотип (SVG) + бренд-иконки соцсетей (lucide убрал брендовые)
- [x] Подключить Prisma **7** + SQLite (`prisma.config.ts`, клиент → `generated/prisma`)
- [ ] Первая миграция *(перенесена в Этап 1 — вместе с реальными моделями)*

> Сборка и typecheck проходят чисто. Главная-заглушка проверена в браузере — совпадает со стилем макетов.

## Этап 1 — Данные и модели `mydebate-database` #done
- [x] Модель User (роль, профиль, + NextAuth Account/Session/VerificationToken/PasswordResetToken)
- [x] Модель Tournament + статусы (String-«enum», SQLite не поддерживает enum)
- [x] Модель TournamentSection (доп. разделы)
- [x] Модель Registration (участие)
- [x] Модель Favorite (избранное)
- [x] Модель Notification (уведомления)
- [x] Модель SupportTicket (обращения)
- [x] `lib/enums.ts` (TS-константы «enum» + хелперы языков)
- [x] Singleton `lib/prisma.ts` (адаптер better-sqlite3)
- [x] Первая миграция `init` применена, `dev.db` создан
- [x] Seed: 3 юзера (пароль `password123`), 11 турниров (10 PUBLISHED + 1 PENDING), комитеты, регистрации/избранное/уведомления

> validate / migrate / seed / build — зелёные. Рантайм-проверка через singleton прошла (3 юзера, 11 турниров, 4 города).

## Этап 2 — Авторизация `mydebate-auth` #done
- [x] NextAuth v5 (Auth.js): Credentials email + пароль (Google-кнопка disabled)
- [x] Регистрация с валидацией (email, сила пароля, совпадение, согласие) + автологин
- [x] Хеширование паролей (bcryptjs)
- [x] Сессии (JWT, в токене id + role) + хелперы `getCurrentUser/requireUser/requireRole/requireAdmin`
- [x] Восстановление пароля (токен в БД, «письмо» — заглушка в консоль, страница сброса)
- [x] Ролевые гейты в `proxy.ts` (Next 16: не middleware): гость → /login, ADMIN-гейт → /403
- [x] Страницы /login /register /forgot-password /reset-password (по макетам) + мин. /profile

> Проверено в браузере: редирект гостя, вход admin, выход, верстка логина совпадает с макетом.

### ✅ Баг/задача (обнаружено при проверке) #done #bug
- [x] **Навбар не меняется после входа** → закрыт в Этапе 3 (session-aware Navbar: гость → Войти/Регистрация; вошедший → Создать турнир / Профиль / Выйти; ADMIN → + Админ-панель). Проверено QA в браузере.

## Этап 3 — Публичные страницы `mydebate-frontend` #done
- [x] **Session-aware Navbar** — после входа: Создать турнир / Профиль / Выйти (+ Админ-панель для ADMIN); закрывает баг Этапа 2 #bug
- [x] Главная (6 блоков: Hero, Ближайшие турниры *(реальные из БД)*, О платформе, Преимущества, Как работает, CTA)
- [x] О нас
- [x] Помощь (FAQ-аккордеон + живой поиск)
- [x] Правила (3 секции)
- [x] Контакты
- [x] Страницы-заглушки /privacy, /terms (футер не в 404)
- [x] Страница Логин *(готова в Этапе 2)*
- [x] Страница Регистрация *(готова в Этапе 2)*

> build/typecheck зелёные. QA в реальном браузере: навбар по сессии (десктоп/мобайл/выход), все публичные страницы 200 гостю, FAQ-аккордеон+поиск, консоль без ошибок, гейты доступа работают. Карточка турнира вынесена в переиспользуемый компонент (задел на Этап 4).

## Этап 4 — Ядро пользователя `frontend + backend` #done
- [x] Каталог «Все турниры»: поиск без перезагрузки *(по названию/городу/организатору, дебаунс, URL-query)*
- [x] Фильтры: формат / тип / город / уровень / дата (комбинируются, «Сбросить», переживают перезагрузку)
- [x] Пагинация + состояния (loading / empty / error)
- [x] Карточка турнира (hero, детали, разделы/комитеты, организатор, кнопка регистрации + состояние дедлайна, 404)
- [x] Регистрация на турнир (простая форма без шага оплаты, валидация, блок после дедлайна + анти-дубль, экран благодарности)
- [x] Избранное (иконка ❤️ на карточке/детали, вкладка профиля, оптимистично)
- [x] Профиль/кабинет: вкладки Мои турниры, Заявки, Избранное, Уведомления, Настройки *(редактирование настроек — заглушка, экшена ещё нет)*

> build/typecheck зелёные. QA в реальном браузере нашёл 6 багов — 5 починены (крит.: экран благодарности после регистрации был недоступен из-за гонки ре-рендера; поиск не искал по городу; фейк-заполнение телефона; навбар на планшете ~768–880px; сырая zod-ошибка языка). Осталась косметика на Этап 7: страница несуществующего турнира отдаёт HTTP 200 вместо 404 (UI верный). В `Registration` добавлены поля формы (миграция `20260707153408_registration_form_fields`).

## Этап 5 — Создание турнира `frontend + backend` #done
- [x] 3-шаговая форма с прогресс-баром
- [x] Шаг 1: основная информация *(формат, тип, языки, город обязателен для OFFLINE, даты с кросс-валидацией, цена)*
- [x] Шаг 2: описание (мин. 50) + загрузка обложки/логотипа *(Route Handler, до 5МБ, jpeg/png/webp)* + динамические разделы (до 20)
- [x] Шаг 3: контакты + тип регистрации *(externalUrl обязателен/валиден при EXTERNAL)*
- [x] Отправка на модерацию (статус PENDING) + уведомления автору и админам
- [x] Вкладка профиля «Турниры, которые вы организуете» (`listMyTournaments`) со статус-бейджами

> build/lint зелёные. QA в реальном браузере — всё ✅ (гейт гостя, валидация всех 3 шагов, успешная отправка → PENDING в БД, не виден в каталоге, виден в профиле, уведомление автору, загрузка файлов на диск, адаптив 390/1400). Один React-warning (`useActionState` вне транзакции) починен через `startTransition`.

## Этап 6 — Модерация + админка `frontend + backend` #done
- [x] Dashboard (счётчики: пользователи, организаторы, турниры + разбивка PENDING/PUBLISHED/HIDDEN, регистрации, открытые обращения)
- [x] Таблица турниров: одобрить / отклонить (с обязательной причиной) / скрыть / удалить (мягко → DELETED), фильтр статуса + поиск + пагинация
- [x] Автосмена роли на Организатор после 1-го одобрения (`maybePromoteToOrganizer` вызывается в `approveTournament`) — проверено в БД
- [x] Пользователи (поиск/фильтр по роли, блокировка/разблокировка, удаление; защита «нельзя себя/другого ADMIN»)
- [x] Организаторы (список + статистика по статусам турниров)
- [x] Обращения (просмотр, ответ → ANSWERED + уведомление, закрытие → CLOSED)
- [x] Внутренние уведомления (TOURNAMENT_PUBLISHED/REJECTED/HIDDEN/DELETED, SUPPORT_TICKET_ANSWERED)

> build/lint зелёные. QA в реальном браузере — всё ✅: гейт /admin для не-админа → /403, дашборд, очередь модерации, **критичный сценарий автоповышения роли USER→ORGANIZER при одобрении (проверен в dev.db)**, отклонение с причиной, скрытие/мягкое удаление, блокировка выкидывает активную сессию, ответ/закрытие обращений. Нативных confirm/alert нет (инлайн-подтверждение). 2 косметических нита (склонение «заявка», подзаголовок организаторов) — починены. После тестов БД пересеяна в чистое демо-состояние.

## Этап 7 — Полировка `mydebate-qa` + `mydebate-reviewer` #done
- [x] Код-ревью всего приложения (критичных дыр доступа НЕТ; авторизация/владелец/гейты консистентны)
- [x] Сквозная проверка user-flow (QA в реальном браузере) — happy-path пользователя и организатора
- [x] Все состояния интерфейса (+ добавлены root `not-found.tsx`, `error.tsx`, `loading.tsx` для /profile и /tournaments/create)
- [x] Анимации (`.animate-fade-in` со стаггером + `prefers-reduced-motion`; hover-карточки; каталог, дашборд, списки модерации)
- [x] Адаптив (390/768/1024/1440 без overflow; индикатор прокрутки таб-бара админки на мобиле)
- [x] Доступность (label/aria, focus-стили, клавиатура — спот-чек)

### Багфиксы Этапа 7 #bug #done
- [x] 🔴 Повторный вход/регистрация под активной сессией стирал сессию → гварды на /login,/register + защита в экшенах
- [x] 🟡 Дедлайн регистрации парсился как UTC-полночь (закрывался в начале дня) → трактовка «конец дня» в `isRegistrationOpen`
- [x] 🟡 `registrationType=EXTERNAL` игнорировался → CTA ведёт на внешний URL, внутренняя форма редиректит прочь
- [x] 🟡 Телефон в форме регистрации не преднаполнялся из профиля → пофикшено
- [x] 🔵 Мёртвый код `getTournamentForModeration`, лишние префиксы в proxy.ts, scroll-behavior warning → убраны

### Достроенные фичи из ТЗ (были отложены) #done
- [x] Форма обращений в поддержку на /contacts (`createSupportTicket`, уведомление админам) — раздел «Обращения» больше не пуст в проде
- [x] Редактирование турнира организатором (только описание + разделы + обложка/логотип; даты/цена/место — read-only, через поддержку; строгий контроль владельца)
- [x] Список участников для организатора (`/tournaments/[id]/participants`, снимок полей заявки, контроль владельца)

> build/lint зелёные (24 роута). QA в реальном браузере — ноль регрессий, все фичи и багфиксы подтверждены (включая контроль доступа к edit/participants: не-владелец/гость → notFound). БД пересеяна в чистое демо-состояние.

---

## ✅ MVP готов
Все 8 этапов (0–7) завершены. Приложение покрывает весь флоу ТЗ: каркас, данные, авторизация, публичные страницы, ядро пользователя, создание турниров, модерация+админка, полировка.

### Осознанно вне MVP (бэклог)
- [ ] Kaspi-оплата / шаг чека при регистрации (отложено по договорённости)
- [ ] Google OAuth (кнопка есть, провайдер заглушён — нет тестовых креденшелов)
- [x] Глубокий аудит доступности (контраст, скринридеры) — проведён 2026-07-11, все находки исправлены

---

## Глубокий аудит доступности + починка (2026-07-11) #done #a11y
Подробности: [[progress]].
- [x] Сквозная проверка живого сайта (QA, Playwright) — все флоу PASS
- [x] `aria-describedby` ↔ текст ошибки во всех формах (`FieldError` с `id`)
- [x] `aria-live`/`role=status` для динамики (счётчик каталога, чеклист пароля, успех-экраны) + `role=alert` на инлайн-ошибках
- [x] `role="group"`+`aria-labelledby` для групп кнопок-переключателей
- [x] Кнопка «показать пароль» с клавиатуры (`tabIndex=-1` убран) + `aria-pressed`
- [x] Контраст плейсхолдеров/`text-muted` на светлом фоне → AA
- [x] Focus на шаг мастера + `aria-live`; бургер Escape/focus/`aria-controls`; `aria-current` в шапке; skip-to-content; reduced-motion для spin/pulse
- [x] 🔴 Продукт: вывод обложки/логотипа турнира в карточке и на детали (fallback на градиент)
- [x] 🐛 Баг из QA: очистка Имя/Фамилия/Email на `/register` после ошибки (поля → контролируемые)
- [x] Верификация: `npm run build` + `npm run lint` зелёные, прогон в браузере

---

## Админ: ручная смена роли пользователя (2026-07-11) #done #admin
Подробности: [[progress]].
- [x] `setUserRole` в `lib/actions/admin.ts` (защита «нельзя менять свою роль», валидация, уведомление `ROLE_CHANGED`)
- [x] Селектор роли в `components/admin/user-row.tsx` (оптимистично, задизейблен для себя)
- [x] Проверено в браузере: USER→ORGANIZER→USER, записывается в БД, консоль чистая

### Усиления админа (2026-07-12) #done #admin
Подробности: [[progress]].
- [x] Редактировать любой турнир (админ-обход владельца в `getTournamentForEdit`/`editTournament` + ссылка «Редактировать» в модерации)
- [x] Создавать турнир сразу опубликованным (в обход модерации; инфо-баннер в мастере)
- [x] Добавлять пользователей вручную из админки (`createUserByAdmin` + форма на `/admin/users`)

> Проверено в браузере под admin: правка чужого турнира, баннер публикации, создание пользователя (Тест Тестов/Организатор) — всё ✅. build/lint зелёные. БД пересеяна.

---

## Деплой — шаг 1: правки в коде (2026-08-09) #done #deploy
Подробности: [[progress]] · План: [[deploy]].
- [x] `prisma generate` в `build`/`postinstall` — иначе сборка на CI/Vercel падает
- [x] Загрузка обложек: Vercel Blob при `BLOB_READ_WRITE_TOKEN`, иначе локальный диск (+ хост в `remotePatterns`)
- [x] `trustHost: true` в `auth.ts` — иначе вход на проде падает с `UntrustedHost`
- [x] `.npmrc` (`legacy-peer-deps`) — конфликт `nodemailer@9` с peer-диапазоном next-auth
- [x] `DIRECT_URL` для миграций в `prisma.config.ts`
- [x] `web/.env.example` + исключение в `.gitignore`
- [x] 🐛 Подделанная ссылка в `coverImage`/`logoImage` роняла каталог через `next/image` → whitelist `imageUrlSchema` + тест
- [x] Убран мусор в корне репозитория (`config`, `prefix`, `set`, `export PATH=~`)
- [x] Проверка: build + lint + tsc + 38 тестов — всё зелёное

## Деплой — шаги 2–5: инфраструктура #todo #deploy
Развёрнуто в [[deploy]].
- [ ] Завести прод-Postgres (Neon/Supabase), взять пулерную и прямую строки подключения
- [ ] Создать Vercel Blob-стор, подключить к проекту
- [ ] Домен + Google OAuth redirect URI на прод-домен
- [ ] Транзакционная почта вместо Gmail SMTP (Resend/Postmark)
- [ ] Импорт репозитория в Vercel, **Root Directory = `web`**, заполнить переменные окружения
- [ ] `prisma migrate deploy` по `DIRECT_URL`, завести админа
- [ ] Smoke-тест по ролям на проде + `npm run test:e2e` против прод-URL

---

## Правки заказчика (2026-09-01) #done
Подробности: [[progress]].
- [x] Автоскрытие турниров из каталога после дедлайна регистрации
- [x] Комитеты MUN: карточки с названием и описанием вместо `<select>`
- [x] 🐛 Ссылки на соцсети открывались как путь внутри сайта → `lib/social.ts`
- [x] Поле TikTok у турнира (+ миграция)
- [x] Кликабельная карточка турнира целиком (stretched link)
- [x] Рассылка участникам от организатора (5 шаблонов, аудитория по статусу)
- [x] Даты демо-данных переведены на смещения от сегодня

## Локализация на 3 языка #i18n
Архитектура и грабли: [[i18n]].

### Сделано #done
- [x] Каркас `next-intl`, все маршруты под `app/[locale]/`
- [x] Префикс `as-needed` (русский без префикса — сохранены позиции в поиске)
- [x] Переключатель языка (настоящие ссылки, сохраняет фильтры каталога)
- [x] hreflang, canonical, alternates в sitemap, запреты в robots на 3 языка
- [x] `formatDate(date, locale)` и `formatPriceValue(price, locale)`
- [x] Плюрализация через ICU вместо ручных функций
- [x] Шапка, подвал, главная, каталог, карточка турнира
- [x] Фильтры каталога, страница турнира, выбор даты
- [x] Вход, регистрация, восстановление и сброс пароля
- [x] Сообщения валидации (63 шт.: схемы + рукописные в экшенах)
- [x] Регистрация на турнир (3 шага, оплата, комитеты)
- [x] Личный кабинет (6 вкладок, настройки, смена пароля)
- [x] Юридические тексты оставлены на русском + пометка на kk/en
- [x] `/kk` и `/en` закрыты от индексации до готовности перевода
- [x] Тесты: словари, ключи схем, прокси, e2e на переключение языков
- [x] 🐛 Прод лёг: next-intl нельзя звать из-под обёртки `auth()`
- [x] 🐛 `/sitemap.xml` уезжал под префикс локали и отдавал 404
- [x] 🐛 Заголовок налезал на ссылку — казахский длиннее русского

### Сделано 2026-09-02 #done
- [x] «О нас», «Контакты», «Помощь» — вся индексируемая часть сайта
- [x] FAQ: тексты в словаре, поиск фильтрует переведённые строки
- [x] Мастер создания турнира (3 шага) и форма редактирования
- [x] Список участников, карточка заявки, панель рассылки с шаблонами
- [x] Админ-панель: обзор, модерация, пользователи, организаторы, обращения
- [x] Страницы ошибок: 403, 404, «турнир не найден», ошибка загрузки
- [x] 🐛 Казахские даты в браузере: Chrome заявляет kk-KZ, но месяцев не знает → своя таблица
- [x] 🐛 Шапка ломалась на казахском: whitespace-nowrap кнопкам, порог lg → xl
- [x] 🐛 Флейки-тест уходил в skip вместо проверки (ожидание отрисовки)

> **Интерфейс переведён полностью.** Словарь: 800 строк в 28 namespace × 3 языка.
> По-русски осталось только то, что и должно: логи `console.error`, значения
> данных (языки турнира, «Казахстан» в schema.org), юридические документы и
> контент из базы.

### Осталось #todo
- [ ] Шаблоны писем (`lib/email/templates.ts`) — письмо на языке получателя. **Требует решения:** брать язык из профиля (поля сейчас нет) или из локали, в которой человек совершил действие
- [ ] Статусные сообщения экшенов («Турнир не найден» и т.п.), ~130 штук — теперь заметны, когда весь интерфейс переведён

### Блокеры перед открытием языков поисковику #todo
- [ ] **Вычитка казахского носителем** — переводы писал не человек; в первую очередь `meta.*` и `home.*`, они видны в выдаче
- [ ] Вычитка английского
- [ ] После вычитки: вернуть `"kk"` и `"en"` в `SEARCH_INDEXED_LOCALES` (`web/i18n/routing.ts`) — этого достаточно, hreflang и sitemap включатся сами

## Инфраструктура #todo
- [x] 🐛 Кончилось место на сервере: чистка образов перенесена в начало `deploy/update.sh` и сделана адресной
- [x] 🐛 Деплой встал: сервер требовал логин к публичному репозиторию. Оказалось — временный сбой GitHub, но по дороге fetch сделан строго анонимным и НЕкритичным (приложение приезжает образом из GHCR; расхождение конфига печатается в логе)
- [ ] Следить за свободным местом — печатается в каждом логе деплоя (сейчас 4.3 ГБ, занято 78%)
- [ ] Подумать про локальный кеш шрифта: одна сборка упала, когда Turbopack не смог достучаться до Google Fonts
- [ ] Если снова появится `could not read Username` — сначала проверить статус GitHub, и только потом лезть в git config на сервере

